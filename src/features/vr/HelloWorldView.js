import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const HelloWorldView = ({route}) => {
    return (
        <View>
            <Text>Hello World View</Text>
        </View>
    )
}

export default HelloWorldView

const styles = StyleSheet.create({})
